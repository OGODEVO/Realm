# Legacy Agent Demos (Moved)

The demo agent runtime in this folder has been moved to:

- `realm-agents-playground` (separate repository)

This repository is network-focused now.

Legacy launch entrypoints are temporarily kept as stubs and will print a "moved" notice unless you set:

```bash
ALLOW_LEGACY_AGENTS=1
```
